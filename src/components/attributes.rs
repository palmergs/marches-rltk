#[derive(Debug, Clone, PartialEq)]
pub struct Brawn {
    pub curr: i32,
    pub max: i32
}

#[derive(Debug, Clone, PartialEq)]
pub struct Grace {
    pub curr: i32,
    pub max: i32,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Charm {
    pub curr: i32,
    pub max: i32,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Smart {
    pub curr: i32,
    pub max: i32,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Faith {
    pub curr: i32,
    pub max: i32,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Focus {
    pub curr: i32,
    pub max: i32,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Vigor {
    pub curr: i32,
    pub max: i32,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Karma {
    pub curr: i32,
    pub max: i32,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Spirit {
    pub curr: i32,
    pub max: i32,
}